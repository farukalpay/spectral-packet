# Reference Examples

These scripts dig into the core bounded-domain engine using a fixed reference problem.

Use them when you want to inspect:

- forward propagation details,
- modal projection behavior,
- inverse reconstruction against a reference packet,
- or generated figures for the reference case.

They are part of the serious engine surface, but they are not the recommended first-run path for new users.
