# Experimental Examples

These scripts exercise optional or explicitly experimental paths.

Current examples include:

- TensorFlow compatibility workflows
- published transport-dataset benchmarks

They are intentionally separated from the stable example surface so the repository does not blur stable product behavior with optional or under-validated paths.
